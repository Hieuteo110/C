#include <stdio.h>
#include <stdlib.h>
void doc ()
{
	FILE *f;
	char ch;
	f=fopen("output.txt","r");
    while (feof(f)==0)
    {
    	ch=getc(f);
    	printf("%c",ch);
    }
    fclose(f);  
}
int main() 
{
    FILE *f, *g;
    char s;
    int d= 0;
    f=fopen("input.txt", "r");
    while ((s = fgetc(f)) != EOF) 
	{
        if (s == 'A')
            d++;
    }
    fclose(f);
    g=fopen("output.txt", "w");
    fprintf(g, "So ky tu 'A' trong file la: %d\n", d);
    fclose(g);
    doc();
    return 0;
}
