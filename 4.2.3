#include <stdio.h>
#include <string.h>
int main()
{
    char s[100];
    int i,dem=0, check=0;
    printf("Nhap xau: ");
    gets(s);
	for (i=0;i<strlen(s);i++) 
	{
        if (s[i] != ' ') 
        {
            if (check == 0) 
			{
                dem++;
                check=1;
            }
   		}
		else check=0;
    }
    printf("So tieng: %d", dem);
    return 0;
}
